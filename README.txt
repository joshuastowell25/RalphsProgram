Dependencies:
python 3
See requirements.txt for required packages
run:
pip install -r requirements.txt

Relies on an external MariaDB server that has financial securities saved as various 'databases' and 'tables' for each divisor